; RF-DETR 推理配置：控制模型路径、输入尺寸、检测阈值和 mask 后处理方式。
[cnn]
model_path=models/rfdetr.onnx
input_width=560
input_height=560
score_threshold=0.65
mask_threshold=0.5
mask_output=logits
score_activation=sigmoid
exclude_last_class=false
clip_masks_to_boxes=false
